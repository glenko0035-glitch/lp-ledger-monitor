# Use a private Robinhood RPC endpoint in GitHub Actions.
# Keep the real value only in the ROBINHOOD_RPC_URL GitHub Actions secret.
ROBINHOOD_RPC_URL=https://your-rpc-provider.example/v1/your-api-key
