# LP monitoring operating notes

- Owner-approved read-only monitor, heartbeat automation `lp`, hourly, attached to this thread. No trading, signing or private keys.
- Work in this directory. Run `node scripts/refresh.mjs`, then `node scripts/verify.mjs`, then `npm run build`. The refresh writes verified receipts, checkpoint, ledger, snapshot and valid APR samples. No API keys required; RPC may rate-limit.
- Cutoff: 2026-09-08 15:49:47 Asia/Shanghai, Unix 1788853787, first eligible block 57524674. Before this cutoff only the 23 original entries and 4 explicitly supplied hashes are included. Never reset cutoff to the current time. Nonce at cutoff is 45.
- `data/base-ledger.json` is the original 23-row ledger. `data/receipts.json` contains all verified receipts. `data/monitor-state.json` is the incremental cursor; checkpoint hash detects reorgs. Keep these files and their history.
- Discovery uses indexed wallet sender/recipient event topics, includes approvals, plus outgoing nonce coverage and native ETH balance residual checks. This is NOT a complete native/internal transaction indexer. Unknown events, missing nonce, unexpected native balance change or liquidity mismatch must be investigated and displayed as unreconciled. Never certify a full P&L in these states. Receipt failures must not advance checkpoint. For arbitrary new pools/swaps/deposits, extend classification only after decoding and verifying. Future external transfers must update capital once, not be treated as fees.
- Latest same-block V4 storage reads use official StateLibrary offsets. Pool3 id 0x4be9657ec9002e528f4f17a5c43edc525a07f888f7b180c2afbf75e096c4f38a, position 1988906, range -284340/-270480, manager 0x8366a39cc670b4001a1121b8f6a443a643e40951. Verify actual liquidity against known events. LP quantities use floating point for display valuation; feeGrowth math uses BigInt.
- The latest two operations: collect 65.80859514007929 PONS +46.532619 USDG, then wallet debit 79.74365073448517 PONS +25.521450 USDG. Added liquidity 182532733013523. Last swap price before add .6809614695076043 implies gross LP add ~84.912876137 PONS+28.366606 USDG; difference is fee credit inside add. Do not double count it.
- Pool1 and pool2 closed valuations retained. Pool3 gas repriced at snapshot ETH/USDG, closed gas at historical exit prices. This is marked valuation, not USDG paid. Incoming exchange gas is paid by sender and not deducted; CEX withdrawal fees already counted in front cost.
- Portfolio external USDT 11334.61, USDG net arrival11325.242392, front cost9.367608. Front cost is embedded in gross original cost. Do not subtract twice. Pool1 includes front costs; global denominators use original USDT plus wallet-paid gas.
- APR: official Uniswap interface calculateApr = round(24h LP fees*365)/round(TVL)*100; fixed V4 LP fee on-chain 3000=0.30%, no hook; protocol fee packed500 each direction. GraphQL sometimes returns blended tier3499. Use verified LP tier, disclose difference; do not claim exact official displayed parity. Source https://interface.gateway.uniswap.org/v1/graphql, v4Pool chain ROBINHOOD, cumulativeVolume(DAY), totalLiquidity. API volume currently missing. Missing APR is null, not0, no fabricated history. Rewards not included. Maintain valid hourly samples only.
- On new transaction/data discrepancy/out-of-range transition notify the user concisely. Otherwise quiet. Once daily after 12:00 Beijing evaluate paired claimed fees vs sqrt(2*C*P), using next add cost, label conditional estimate; only notify on newly reaching threshold, not every hour. Record notification state separately in data/notification-state.json to avoid duplicates.
- Publish current snapshot to existing owner-private Sites project appgprj_6a9fb34091bc819181651caf8dfd170d, URL https://lp-ledger-monitor.glen2212.chatgpt.site. User requested continuing monitoring and dashboard updates. Follow Sites building/hosting skills: verify owner-only, commit and push exact source with fresh ephemeral credential, full SHA, package-site.sh, save version, private publish, poll terminal. Never expose token or broaden sharing. No browser opening during background heartbeat. Local desktop availability and account usage affect scheduled execution; no claim of independent cloud daemon.
- Failed reads must preserve last successful data, record error to data/last-error.json and notify persistent failures. A snapshot older than two hours is stale. If source writes exist after a failed deployment, retry publishing these after validation instead of silently losing updates.

- Run node scripts/assess.mjs after refresh and verification; it stores daily assessment and duplicate-notification state. First baseline has no historical repeat alerts. Alert output guides user notifications.
- Six additional transactions at 19:55–19:56 were verified: three ERC20 approvals, one USDG1000→PONS1355.804413 swap, collect, increase liquidity. Approved swap hash is explicitly classified in refresh. Strategy-to-pool3 allocation draws only the USDG shortfall after known flows from the original strategy cash reserve. This affects pool3 capital/ROI but not portfolio external capital or total P&L; remaining pool3 USDG attributable balance is floored to zero by that transfer. Show this accounting policy. Native balance residual is cumulative until explicitly reconciled.
